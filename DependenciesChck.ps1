#Meant to run once at the beginning.
#Checks for dependencies: ImageMagick (magick), 7-zip (7z), ffmpeg (ffmpeg)
#Checks for $PATH to those dependencies
#Create parameters file for repeated runs: archive password, archive size, files per folder, excluded extensions, included movie extensions

#To be made.
